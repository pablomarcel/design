# pytest.ini (repo root)
[pytest]
# Where to look for tests
testpaths =
    01_intro/tests
    02_materials/tests
    03_load/tests
    04_deflection/tests
    05_failureStatic/tests
    06_failureDynamic/tests
    07_shafts/tests
    08_screws/tests
    09_welding/tests
    10_springs/tests
    11_rollingBearings/tests
    12_journalBearings/tests
    13_gears/tests
    14_spurGears/tests
    15_bevelGears/tests
    16_clutches/tests
    17_flexible/tests
    18_transmission/tests
    19_fea/tests
    20_gdt/tests

# Default options:
# -vv : very verbose test names
# -ra : show summary of skips/xfails
# --durations=10 : slowest 10 tests
# pytest.ini
addopts =
    -vv
    -ra
    --durations=10
    --color=yes
    --cov=01_intro
    --cov=02_materials
    --cov=03_load
    --cov=04_deflection
    --cov=05_failureStatic
    --cov=06_failureDynamic
    --cov=07_shafts
    --cov=08_screws
    --cov=09_welding
    --cov=10_springs
    --cov=11_rollingBearings
    --cov=12_journalBearings
    --cov=13_gears
    --cov=14_spurGears
    --cov=15_bevelGears
    --cov=16_clutches
    --cov=17_flexible
    --cov=18_transmission
    --cov=19_fea
    --cov=20_gdt
    --cov-report=term-missing
    -o console_output_style=classic
    -o faulthandler_timeout=60

# Live logging to console (use logging.* in tests)
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S

# Make xfail strict so accidental passes are flagged
xfail_strict = true

# Register custom markers
markers =
    cf: closed-form / symbolic tests
    symbolic: tests requiring SymPy
    slow: slow-running tests
    batch: batch job tests

# Tame 3rd-party deprecation noise but keep our own strict
filterwarnings =
    error::DeprecationWarning:transientAnalysis\..*
    error::DeprecationWarning:rootLocus\..*
    ignore::DeprecationWarning
